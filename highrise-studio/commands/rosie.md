---
description: Initialize a Highrise Studio project with everything Claude needs.
allowed-tools: highrise-studio:setup-highrise-studio-project
---

# Rosie

When a user executes the `/rosie` command, they want to set up the current working directory -- a Highrise Studio project -- to be ready for use by Claude Code. You will immediately invoke the `setup-highrise-studio-project` skill, and do nothing else.

