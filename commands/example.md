# Example Command

This is an example slash command. When a user types `/example`, this prompt will be executed.

You can write any instructions here for Claude to follow when this command is invoked.

## Usage

```
/example
```

## What this command does

Replace this content with your actual command logic. You can:
- Give Claude specific instructions
- Set up context for a particular workflow
- Create shortcuts for common tasks
